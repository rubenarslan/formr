```{r eval=TRUE,echo=FALSE}
if (!exists("indent")) {
	indent = '###' # ugly hack so _regression_summary can be "spun" (variables included via `r ` have to be available)
}
if (exists("testing")) {
	scale = 1:10
}
```

`r indent` `r attributes(scale)$scale` {#`r attributes(scale)$scale` .tabset}

`r indent`# Reliability {#`r attributes(scale)$scale`_reliability}
```{r}
attributes(scale)$reliability
```

`r indent`# Likert plot {#`r attributes(scale)$scale`_likert}
```{r}
opts_chunk$set(fig.height = ncol(attributes(scale)$likert_plot$items))
```
```{r}
graphics::plot(attributes(scale)$likert_plot)
```

`r indent`# Items {#`r attributes(scale)$scale`_items}
```{r}
pander::pander(dplyr::bind_rows(
	lapply(attributes(scale)$item, function(x) { 
		x$label_parsed = x$choices = x$choice_list = NULL
		as.data.frame(t(x)) })
))

```
