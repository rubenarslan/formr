% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_to_formr.R
\name{formr_default_dir}
\alias{formr_default_dir}
\title{Get or set the default directory for downloads and backups}
\usage{
formr_default_dir(dir = NULL)
}
\arguments{
\item{dir}{a single directory path to use as the default. If \code{NULL} (the
default) the stored value is returned unchanged. The directory itself is
created on first write if it does not yet exist.}
}
\value{
The current default directory as a length-1 character string, or
\code{NULL} when none has been set.
}
\description{
formr's file-writing functions (e.g. \code{\link[=formr_backup_study]{formr_backup_study()}},
\code{\link[=formr_api_backup_run]{formr_api_backup_run()}}, \code{\link[=formr_api_pull_project]{formr_api_pull_project()}}) never write to your
working directory by default. Instead they default their destination to the
value stored here, which is unset (\code{NULL}) until you choose one — so nothing
is ever written until you opt in. Call this function with a path to set a
session-wide default, or without arguments to read the current value. The
path is held in memory for the current R session only; nothing is written to
disk to persist it.
}
\examples{
formr_default_dir(tempdir())
formr_default_dir()
}
