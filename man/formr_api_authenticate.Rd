% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_core.R
\name{formr_api_authenticate}
\alias{formr_api_authenticate}
\title{Authenticate with formr}
\usage{
formr_api_authenticate(
  host = "https://rforms.org",
  client_id = NULL,
  client_secret = NULL,
  access_token = NULL,
  account = NULL,
  verbose = TRUE
)
}
\arguments{
\item{host}{API Base URL. Defaults to \code{.formr$host} when running on
rforms.org, otherwise \code{"https://rforms.org"}.}

\item{client_id}{OAuth Client ID.}

\item{client_secret}{OAuth Client Secret.}

\item{access_token}{Direct Access Token.}

\item{account}{Optional string identifier for multiple accounts on the same host.}

\item{verbose}{Logical. If TRUE (default), reports success via \code{\link[=message]{message()}}.}
}
\value{
Invisibly \code{NULL}; called for its side effect of obtaining and caching an OAuth access token (errors on failure).
}
\description{
Connects to the API. If no credentials are provided, the auto-pickup
chain is: the package's hidden \code{.formr} env (set automatically when
the code runs inside an OpenCPU session on rforms.org), then the
calling-frame chain (for legacy injectors that wrote bare locals into
the wrapper scope), then the keyring.
}
