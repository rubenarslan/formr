% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_overview.R
\name{formr_overview_sankey}
\alias{formr_overview_sankey}
\title{Render a participant-flow Sankey for an overview script}
\usage{
formr_overview_sankey(
  run_name = .formr$run_name,
  testing = FALSE,
  orientation = "v",
  min_avg_visits_to_annotate = 1.1
)
}
\arguments{
\item{run_name}{Name of the run. Defaults to \code{.formr$run_name},
which formr.org sets when an OverviewScriptPage renders.}

\item{testing}{If \code{FALSE} (default), only real participants are
included. \code{TRUE} to include only test sessions, \code{NULL} for both.}

\item{orientation}{Sankey orientation; \code{"v"} (default) renders
top-to-bottom (readable on narrow admin pages), \code{"h"} renders
left-to-right.}

\item{min_avg_visits_to_annotate}{Threshold above which a node's
label gets the "avg N visits" suffix. Default 1.1 — slightly above
exactly-once so single-pass runs stay clean.}
}
\value{
A plotly Sankey object, or \code{NULL} with a message when there
are no rows to plot. Returning \code{NULL} lets the caller's knitr chunk
gracefully display the message instead of erroring.
}
\description{
Pulls the run's per-unit interaction history via
\code{\link[=formr_api_unit_sessions]{formr_api_unit_sessions()}} and renders a plotly Sankey diagram of
how participants are moving through the ordered units.
}
\details{
Sankey diagrams can only draw acyclic graphs, but diary / longitudinal
studies revisit the same units across iterations. To stay readable
without losing information, the helper collapses each position to a
single node (counting only the first time a participant visits it)
and surfaces the average per-participant visit count as an "avg N
visits" suffix on the node label. Single-pass runs stay clutter-free
(no suffix when the average is ~1); diary studies show e.g. "p20:
Daily mood (avg 14.2 visits)" so the loop density is visible without
drawing it.

Designed to be called from an OverviewScriptPage on formr.org, where
the server injects the per-token \code{.formr$access_token} / \code{.formr$host}
/ \code{.formr$run_name} environment and \code{\link[=formr_api_authenticate]{formr_api_authenticate()}} picks
them up automatically. Outside an Overview render, set
\code{run_name} explicitly and call \code{\link[=formr_api_authenticate]{formr_api_authenticate()}} first.
}
