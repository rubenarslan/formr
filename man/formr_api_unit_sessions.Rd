% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_sessions.R
\name{formr_api_unit_sessions}
\alias{formr_api_unit_sessions}
\title{List Per-Unit Sessions in a Run}
\usage{
formr_api_unit_sessions(
  run_name,
  session_codes = NULL,
  testing = NULL,
  since = NULL,
  limit = 1000,
  offset = 0
)
}
\arguments{
\item{run_name}{Name of the run.}

\item{session_codes}{Optional character vector — restrict to one or
more participants' histories.}

\item{testing}{Filter: TRUE for test sessions only, FALSE for real
participants only, NULL for both.}

\item{since}{Optional ISO 8601 datetime string. Returns only unit
sessions whose \code{created} is at-or-after this — handy for
incremental polling.}

\item{limit}{Pagination limit (default 1000, max 10000).}

\item{offset}{Pagination offset (default 0).}
}
\value{
A tidy tibble with columns: \code{unit_session_id}, \code{session},
\code{testing}, \code{unit_id}, \code{unit_type}, \code{unit_description}, \code{position},
\code{iteration}, \code{created}, \code{expires}, \code{ended}, \code{expired}, \code{result},
\code{state}.
}
\description{
Returns one row per (participant × unit × iteration) for the run — the
history view that complements \code{\link[=formr_api_sessions]{formr_api_sessions()}} (which gives one
row per participant with their \emph{current} unit only).
}
\details{
Use this for trajectory plots (Sankey, alluvial), drop-off analytics,
and debugging stuck participants. The rows arrive ordered by
\verb{(session, created, unit_session_id)}, so \code{dplyr::group_by(session) |> dplyr::mutate(next_unit = dplyr::lead(unit_description))} gives the
edges of a trajectory plot directly.

Special units (OverviewScriptPage, ServiceMessagePage, ReminderEmail)
surface with \code{position = NA} because they live outside the ordered
run flow.
}
